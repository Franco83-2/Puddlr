    <%= flash[:message] if flash[:message] %>
    <h1>
          <%= "Info for the selected vehicle:" %> <br />    </h1>
              <%= "#{@car.make}" %> <br />
          <ul>
          <li><%= @car.model %></li>
          <li><%= @car.year %></li>
          <li><%= @car.color %></li>
          </ul>
          <%= link_to "Edit Vehicle", edit_car_path(@car.id) if session[:user_id] == @car.user.id %><br />
